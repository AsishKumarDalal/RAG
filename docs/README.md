# RAG Harness — Docs

* `architecture.md` — full system detail: components, config, indexing pipeline, query pipeline, prompt contract, highlight logic, benchmarking. Start here.
* `indexing.md` — indexing in very detail with real examples (load → split → tag → embed → store → query-time use).
